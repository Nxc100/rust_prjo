# 视觉海岸批量处理工作台 · 项目现状与架构分析

> 文档日期：2026-06-29　|　对应版本：`visual_coast_batch` **v1.2.1**（可投产 / 已分发）
> 用途：接手项目时的全面说明——现在是什么、做什么、用什么模型、到哪一步了。
> **最新「现在完成了什么 / 接下来干什么」以文末 §17（2026-06-29）为准**（依次取代 §14 / §15 / §16）。

---

## 0. 一句话定位

一个 **Windows 桌面 GUI 程序**（纯 Rust，单文件 exe，零运行时依赖），给婚纱影楼修图师做**批量 AI 出图**。当前正从「单一能力」扩展为「**多能力工作台**」：

- **能力一（已成熟）**：RunningHub 批量**精修 / 高清放大**——把客户原片成批跑 ComfyUI 工作流。
- **能力二（整合中）**：**人物入景**——把真人原片放进固定场景库，自动判景别→选景→统一调色，用 AI 出成套写实婚纱样片。

两者将共用同一套界面外壳（图片列表、前后对比、进度、日志、账户），并可串成一条龙：人物入景出图 → RunningHub 高清放大。

---

## 1. 当前状态总览

| 模块 | 状态 | 说明 |
|---|---|---|
| **RunningHub 批量精修/放大** | ✅ **可用、成熟**（v1.0.0） | 完整功能，已能打包分发 |
| **预览放大清晰度修复** | ✅ 已修 | 对比预览改为全分辨率纹理，放大不再发虚 |
| **人物入景 · M1 场景大脑** | ✅ 完成并验证 | `cmode.rs`，与同事 Python `match_scene.py` 逐字对拍通过 |
| **人物入景 · M2 出图后端** | ✅ 完成并验证 | `foursapi.rs`，真连 4sapi `gpt-image-2` 出图成功 |
| **人物入景 · M3 状态机** | ✅ 完成并验证 | `cstate.rs`，客户 manifest、选景排重、复用变构图、QC 流转、归集、持久化 |
| **人物入景 · M4 模式 UI（L1）** | ✅ 完成并验证 | 顶部模式切换 + C 模式配置/单子列表/QC 预览；建账→排片→装配→出图→选片→归集全程点界面 |
| **人物入景 · M5 一条龙** | ⏳ 待做 | C 出图 → 送 RunningHub 放大 |

> **现状**：「人物入景」已作为**第二个模式**接入界面——顶部可在「RunningHub 精修/放大 ｜ 人物入景」间切换。L1 全链路（操作员全程点界面跑通一个客户）已打通；真人端到端出图效果已验证（见 §14.1）。

测试：`cargo test` **36 个自动化测试全绿**（共 40 个测试函数）+ **4 个需联网的 live 测试**（出图烟测 / 真人端到端 / AI 判景别 / 查额度，默认 `#[ignore]` 跳过，手动验证已通过）。

---

## 2. 使用的模型

本程序**自身不含 AI 模型**，而是调用云端服务出图。涉及两个后端、两类模型：

| 能力 | 后端 / 接口 | 模型 | 说明 |
|---|---|---|---|
| **精修 / 高清放大** | RunningHub OpenAPI（`https://www.runninghub.cn`） | 由用户选定的 **ComfyUI 工作流**决定 | 示例工作流为 **Flux2-Klein 人像精修 + 高清放大**；程序自动识别工作流的 `LoadImage`/`SaveImage` 节点与可调参数（KSampler 种子/步数/CFG/重绘、CLIPTextEncode 提示词、ImageScaleBy 放大倍数） |
| **人物入景 · 出图** | 4sapi（`https://4sapi.com`），端点 `/v1/images/edits` | **`gpt-image-2`** | OpenAI 系图像编辑模型，经 4sapi 中转（Codex 上游）。**双图编辑**：图A人物 + 图B场景 plate，按装配好的提示词重绘合成 |
| **人物入景 · 景别判定** | 4sapi，端点 `/v1/messages`（Claude 原生格式，同令牌） | **`claude-sonnet-4-6`** | **已接入一键流**：点「开始」自动逐张判景别+人数（`foursapi::judge_shot`，缩略图 base64 上传，结构化 JSON 输出）；操作员可在左侧手改后重跑。AI 预筛 QC 仍待做（同模型） |

> 模型选择原则：构建 AI 能力时优先用最新最强模型；当前出图锁定 `gpt-image-2`（同事系统实测此模型出图协调、可双图锁脸）。

---

## 3. 技术栈

- **语言**：Rust 2021，工具链 `stable-x86_64-pc-windows-gnu`（GNU 工具链，自带链接器，无需 MSVC / Windows SDK）。
- **GUI**：`eframe` / `egui` 0.34（立即模式 GUI，默认 wgpu/DX12 渲染）。
- **网络**：`reqwest` 0.12（blocking + multipart + json，TLS 走系统 Schannel `default-tls`，不依赖 OpenSSL）。
- **其它**：`serde`/`serde_json`（配置与接口）、`rfd`（原生文件对话框）、`crossbeam-channel`（后台→UI 消息）、`image`（纯 Rust 缩略图解码）、`base64`（解码 4sapi b64 出图）、`anyhow`（错误）。
- **构建期**：`winresource`（嵌入 exe 图标）。
- **测试**：`egui_kittest`（无头模拟真实控件树点击/渲染）。
- **产物**：单个自包含 `.exe`（约 9.4 MB），Win10/11 双击即用，**零运行时依赖**（无 VC++/MinGW/.NET/WebView2/OpenSSL）。

---

## 4. 代码结构与模块职责

```
rust_prjo/
├── Cargo.toml / build.rs / rust-toolchain.toml / build.ps1
├── key.txt                      # 4sapi 密钥（已 gitignore，绝不入库）
├── src/
│   ├── main.rs        (~4800 行) 界面 + 状态 + 后台编排 + RunningHub 批处理 + 人物入景 UI
│   ├── api.rs                    RunningHub OpenAPI 客户端（上传/提交/轮询/下载/账户）
│   ├── workflow.rs               解析 ComfyUI 工作流 JSON，识别输入/输出节点 + 可调参数
│   ├── cmode.rs       【新】      人物入景「大脑」：选景 + 四轴提示词装配（移植 match_scene.py）
│   ├── foursapi.rs    【新】      4sapi gpt-image-2 双图出图客户端
│   └── cstate.rs      【新】      人物入景状态机：客户 manifest + 选景排重 + QC 流转 + 归集
├── assets/wedding/    【新】      人物入景的可热插拔数据（含 18 张 plate jpg）
│   ├── templates/人物入景_双图暖金版.md    出图提示词模板（机器可读区块）
│   ├── scenes/catalog.json                场景库目录（18 张 plate 的标签/构图）
│   ├── scenes/series_profiles.json        场景配置卡（四轴拆开的“场景专属话”）
│   └── _golden/                           与 Python 对拍的黄金样本（测试用）
├── doc/                          实现方案、打包指南、本文档、原始 rh_batch.py、样片
└── tips/                         4sapi 接口文档（gpt-image-2 / edits / 图片理解 / claude）
```

**各模块要点**

- **`main.rs`**：三栏布局（顶部控制+进度+设置 / 左侧列表 / 中间对比 / 底部日志）。含可持久化配置（预设 `Preset` + 窗口布局 `Store`，存 exe 同目录 `vc_batch_config.json`）、后台线程池跑批、对比查看器（并排/滑动、缩放平移、全分辨率）、清单 CSV。**顶部 `AppMode` 模式切换**：RunningHub 与人物入景共用同一外壳，各自的头部/列表/预览按模式分支（`w_header`/`w_list`/`w_preview` + 背景出图线程）。
- **`api.rs`**：`RhClient` —— `upload_image` / `create_task`（解析队列占满、节点校验提示）/ `poll_once`（完成/排队/不确定三态）/ `download` / `account_status`（余额、并发占用）。含网络重试退避。
- **`workflow.rs`**：`detect_io_nodes`（唯一 LoadImage→输入、唯一 SaveImage→输出）+ `detect_params`（从 widgets_values 提取常见可调参数，做成表单）。
- **`cmode.rs`【新】**：`SceneEntry`/`PersonAttrs` 数据结构；`load_from_dir` 加载模板/目录/配置卡；`can_serve`/`rank_candidates`（景别层级：宽景别图可借给窄景别）；`auto_pick_scene`/`attr_score`（最配优先 + 复用排重）；`assemble_prompt`（**四轴正交装配**：景别×调色×场景概念×plate）；`size_for_aspect`（按原片宽高比选出图尺寸）。
- **`foursapi.rs`【新】**：`FsClient::edit_dual`（双图 multipart 上传出图）；响应兼容 `b64_json` 与 `url` 两种回传；**瞬时错重试 / 持久错响亮中止**（纯 API 命门）。
- **`cstate.rs`【新】**：`CManifest`/`CJob`/`CStatus`（pending→prompted→awaiting_qc→selected→done｜qc_fail）；`init`（扫文件夹建账）、`set_job`（改计划字段+失效回退：宽→窄保留场景、窄→宽清空重选）、`auto_plan`（自动选景排重）、`assemble`（调 cmode 装配+复用即变构图）、`select`/`fail`/`collect`（选片+需放大标记+归集）、JSON 持久化。

---

## 5. 功能清单

### 5.1 RunningHub 批量精修 / 高清放大（现有，可用）

- **配置表单**：API Key、工作流 ID、输入/输出文件夹、工作流 JSON、并发数（1–8）。
- **配置预设**：多套参数命名保存、切换、增删改（如「精修」「4K 放大」各一套）。
- **灵活导入**：选整个文件夹，或 **`选图片` 对话框单选/多选若干张**，或直接**拖入**文件夹/图片；散图与文件夹叠加去重一起跑。
- **工作流智能识别**：读 ComfyUI 工作流 JSON 自动识别输入/输出节点；**参数表单化**（种子/步数/CFG/重绘/提示词/放大倍数勾选覆盖）。
- **批量执行**：后台线程池并发、断点续跑（已有 `原名_rh*` 结果自动跳过）、停止、失败重试 / 重跑选中。
- **逐图进度**：等待/上传/提交/生成/下载/完成/跳过/失败八阶段，状态徽章 + 列表筛选 + 跟随处理中。
- **前后对比查看器**：并排 / 滑动对比，缩放平移、1:1、**全分辨率**（刚修复，放大不发虚）。
- **账户与预警**：常驻余额/并发徽章、测试连接、跑前余额预警（按每张预估消耗）。
- **实时日志**：级别过滤 + 搜索 + 导出。
- **收尾**：完成横幅汇总、清单 `_manifest.csv`、任务栏闪烁 + 提示音 + 自动打开输出夹。

### 5.2 人物入景（L1 已可用 · 顶部模式切换进入）

- **四轴解耦装配**（M1 ✅）：景别(SHOTS) × 调色(KEY：自然/暖/冷) × 场景概念(配置卡) × 具体 plate(comp_prompt)。加新风格只改数据、不改代码。
- **景别层级选景**（M1 ✅）：宽景别场景图可借给窄景别复用，像「同一次拍摄」；自动选景 + 复用排重 + 复用即变构图。
- **双图出图**（M2 ✅）：图A人物（锁脸/锁姿势/锁衣饰/锁头部朝向）+ 图B场景 plate，4sapi `gpt-image-2` 重绘合成。
- **状态机**（M3 ✅）：客户 manifest、断点续跑、选景排重、改景别失效回退、QC 流转、归集 + 需放大标记。
- **模式界面 · 一键流**（M4 ✅）：顶部切到「人物入景」→ 配置（4sapi Key/调色/场景池/原片·输出文件夹/场景库/**并发数 1–8**/**断点续跑开关**；原片支持**选文件夹或单/多选图片或拖入**叠加）→ **一个「▶ 开始」**。后台自动：**AI 判景别+人数（`claude-sonnet-4-6` 视觉）→ 自动选景排重 → 装配 → 双图出图 → 落到输出夹**。**对齐 RunningHub 的生产能力**：① **并发产出**（判景别/出图各开 N worker 池）；② **断点续跑**（输出按人物原片名 `<原名>_c.png`，已有结果预跳过、不判不出＝省钱，可「■ 停止」）；③ **图片列表筛选**（全部/进行中/成功/跳过/失败）+ 进度条 + 成功/跳过/失败计数。左侧仍可逐单手改景别/人数后重跑；中间原片↔候选全分辨率对比 + 可选 QC 选用/判废/归集。
- **测试连接 + 实时额度**（✅）：`foursapi::account()` 调 `GET /api/user/self`，「测试/刷新」按钮后台查 4sapi 余额并显示「🪙 余额 ≈ $X」。**注意**：该面板接口需 4sapi**系统访问令牌**（个人设置页生成，非出图用的 sk- 密钥）+ 可选用户 ID；故界面单列一个「访问令牌」字段（可留空，留空则用 sk- 查、多半被拒并提示）。出图结束自动刷新额度。
- **待做**：串接 RunningHub 放大（M5）；AI 预筛 QC（L2 剩余项）；C 模式配置持久化 + 滑动对比。

---

## 6. 数据流

**RunningHub 批处理**
```
读输入目录图片 → [线程池] 每张：upload → create_task(注入LoadImage) → poll → download(原名_rh.png)
            → 进度/日志回传 UI（crossbeam channel）→ 写 _manifest.csv → 收尾提醒
```

**人物入景（目标形态）**
```
真人原片 →(人工判)景别+人数+调色 → cmode 自动选景 + assemble_prompt 装配双图提示词
        → foursapi 双图出图(人物+plate, gpt-image-2) → 全分辨率对比 QC → 选片归集
        →(可选)送 RunningHub 高清放大
```

---

## 7. 配置与数据文件

| 文件 | 作用 | 备注 |
|---|---|---|
| `vc_batch_config.json` | 界面配置 + 预设 + 窗口布局 + **人物入景配置**（`wedding`：场景库路径/输出夹/调色/概念池/并发/续跑） | 存 **exe 同目录**（故不能装到 `Program Files` 只读目录）；C 模式配置已**持久化**（设一次记住） |
| `key.txt` | 4sapi 凭据（3 行：sk- / 系统令牌 / 用户ID） | **已 gitignore**，绝不入库 |
| **场景库（局域网共享）** `\\DESKTOP-66773HC\ai_work\wedding_scene_lib\` | `templates/双图暖金版.md` + `scenes/catalog.json`（18 plate：bbsh 11 + jiaoshi 7）+ `series_profiles.json`（配置卡） | **主机维护、全员共用、不随程序打包**；程序默认指向此路径（`SHARED_SCENE_DIR`），可在界面改并记住；**扩库只在共享上改一次、全员生效**。`assets/wedding/` 项目内副本仅供测试与开发 |
| 输出文件夹 / `<原片名>_c.png` / `final/` | 出图结果（按原片名）与归集 | 用户在界面选定；断点续跑按文件名跳过 |

> **场景库部署**：为避免「越打越大、每次随包分发」，场景库已从随程序打包改为**局域网共享单一来源**（`\\DESKTOP-66773HC\ai_work\wedding_scene_lib`）。程序通过 UNC 路径读模板/catalog/配置卡（小文本，加载快）与 plate（出图时后台线程读）。离线机器可把整个 `wedding_scene_lib\` 拷到本地、界面指过去。

---

## 8. 构建与打包

- **编译**（需 GNU 工具链 PATH）：`powershell -ExecutionPolicy Bypass -File build.ps1 [-Release]`
  - 注：在嵌套 `powershell -File` 调用下 build.ps1 的中文会被 PS5.1 按 GBK 误解析报错；CI/脚本里改为手动把 `toolchain\mingw64\bin` + rustup self-contained 目录加进 PATH 后直接 `cargo build`。
- **产物**：`target\release\visual_coast_batch.exe`（+ 中文名副本用于分发）。
- **打包**：绿色 zip / Inno Setup `setup.exe` / MSI（详见 `doc\打包与安装指南.md`）。**务必装到用户可写目录**（LocalAppData），否则配置存不下。
- **签名**：未签名 exe 首次运行触发 SmartScreen，属正常；需消除得买代码签名证书。

---

## 9. 已知约束与风险

**RunningHub 侧**
- 并发数受 apiKey 套餐额度限制；基础套餐填 1，否则频繁「队列占满」。
- 放大任务慢（单张数分钟，超时默认 1800s）。

**人物入景 / 纯 API 侧**（来自同事交接文档）
- **纯 API 无回退**：4sapi 掉线 / 渠道无货（503）/ 余额 0 → 整条线停。建议留备用中转做 API 级回退。
- **共用令牌、余额共享**：跑干整批失败，要有人盯余额、及时充。
- **隐私/合规**：客户人脸照会发第三方中转，对外收费前需想清授权与数据处理。
- **交付分辨率**：默认 1.6MP 不放大；要 2K+ 才人工 Topaz（或本工作台 RunningHub 放大兜底）。放大会把小手锐成畸形、需局部修手。
- **质量边界**：背身/小脸/远景脸仍可能漂 → 走人工贴真脸（B 路线，系统不自动）；plate 必须无水印；全册基调一次定死、别中途换。

**工程侧**
- 人物入景引擎已就绪但**未接 UI**，终端用户暂用不到。
- 预览大图为控显存只驻留当前选中项，来回切换大图会按需重解码（超大图零点几秒延迟）。

---

## 10. 后续路线图

| 里程碑 | 内容 |
|---|---|
| **M3 · 状态机** | 客户 manifest（pending→prompted→awaiting_qc→selected→done）、断点续跑、自动选景排重、Topaz 标记，复用工作台存储/清单 |
| **M4 · C 模式 UI** | 顶部模式切换「RunningHub 精修/放大 ｜ 人物入景」；景别手动下拉 + 人数 + 自动选景（可改）+ 调色档；复用图片列表与**全分辨率对比器**做 QC；选片归集 |
| **M5 · 一条龙** | 人物入景出图（1.6MP）→ 一键送 RunningHub 高清放大 |
| 增强（后续） | 4sapi 视觉模型自动判景别（手动可改）；场景 plate 纳入 assets / 场景库目录配置；运行历史与真实成本核算 |

> 进 UI 前建议：用真人原片做一次 **M1+M2 端到端验证**（装配提示词→双图出图），确认锁脸/锁景别/调色效果达标，再搭界面（`doc/1V3A3079.JPG`、`doc/1V3A3085.JPG` 可作样片）。

---

## 11. 关键文件索引

| 想了解… | 看这里 |
|---|---|
| 界面 / 跑批编排 | `src/main.rs` |
| RunningHub 接口 | `src/api.rs`、`doc/RunningHub桌面程序实现方案.md` |
| 工作流节点/参数识别 | `src/workflow.rs` |
| 人物入景选景与装配 | `src/cmode.rs`、`assets/wedding/` |
| 4sapi 出图 | `src/foursapi.rs`、`tips/gpt-image-2`、`tips/图片编辑edits` |
| 打包分发 | `doc/打包与安装指南.md` |
| 原始 Python 脚本（移植来源） | `doc/rh_batch.py`、同事 skill `wedding-photo-ai/` |

---

# 第二部分 · 走向「完善的高度自动化系统」

> 本部分基于对同事 skill（`wedding-photo-ai`）全部脚本/模板/数据 + 两份交接文档的再次通读，
> 给出把「人物入景」做成**高度自动化生产线**的总体设计，以及**现在完成了什么、接下来干什么**。

---

## 12. 同事系统全貌复看 · 每个部件的自动化归宿

「人物入景」原系统由「编排层（算法）+ AI（出图/判图）+ 人工（QC）」三段构成。下面把它**所有**部件逐一映射到本工作台里的去向——这就是整合的全集。

| 原部件（同事 skill） | 职责 | 在本工作台里 |
|---|---|---|
| `prep_input.py` | 把客户原片拷进 input | ❌ 不移植——UI 直接「选客户文件夹 / 拖拽」（外壳已有） |
| `c_batch.py` 状态机 | manifest 记账/续跑/选景排重/QC流转 | → **M3 原生重写**（复用工作台 store/清单） |
| `match_scene.py` | 选景 + 四轴装配提示词 | ✅ **已移植**为 `cmode.rs`（逐字对拍通过） |
| `run_one.py`（浏览器出图） | 出一张图 | ✅ **已替换**为 `foursapi.rs`（纯 API，gpt-image-2 双图） |
| `qc_sheet.py`（拼对比图） | QC 时拼「原图+候选」对比 | ✅ **已被取代**——工作台**全分辨率交互式对比器**比静态拼图强 |
| `make_grid.py` | 拼四宫格样片 | → M5 可选并入（或保留为后处理） |
| `find_convo/grab_*/start_edge.ps1` | 浏览器通道相关 | ❌ 纯 API 后**不再需要** |
| `selftest.py` | 离线工程自检（编译/编码/数据/装配矩阵/状态机） | → **M3 配套：移植成 Rust 回归测试**（部分已做，见 §13.5） |
| `templates/*.md` | 出图提示词模板（机器可读区块） | ✅ 当**外部数据**读（`assets/wedding/templates/`） |
| `scenes/catalog.json` | 场景库目录（18 plate 标签/构图） | ✅ 当**外部数据**读 |
| `scenes/series_profiles.json` | 场景配置卡（四轴拆开） | ✅ 当**外部数据**读 |
| `scenes/*.jpg`（18 张 plate） | 双图出图的图B素材 | ⏳ **待纳入** `assets/wedding/scenes/`（M3 前必做） |
| `景别判定与QC手册.md` | 判景别线索 + person_attrs + 排片规则 + **11 项 QC 评分单** | → 既是**人工判图依据**，也是**AI 判景别/AI 预筛 QC 的结构化提示词来源**（§13.4） |
| `output/picks/*.jsonl` | 记录「候选+最终选择」 | → **L4 闭环学习**的种子（用人工选片校准选景权重） |

**结论**：编排层（选景/装配/状态机）+ 出图全部可纳入工作台原生；浏览器那一套整体废弃；模板/场景/配置卡当外部数据保留同事的热插拔更新契约；剩下两个**人工触点**（判景别、QC 选片）正是自动化的主战场。

---

## 13. 高度自动化设计

### 13.1 自动化的本质：识别并收敛「人工触点」

整条「人物入景」流水线只有**两个**不可省的人工判断，其余皆可自动化：

```
选客户文件夹 →〔判景别+人数〕→ 自动选景 → 自动装配 → 自动出图 →〔QC 选片〕→ 自动归集 → 自动放大 → 拼样片
              ▲ 人工触点①                                        ▲ 人工触点②
```

- **触点①「判景别」**：交接文档 §4.2 明确**允许用视觉 API 自动判**（判错不致命、可手改）→ 应自动化。
- **触点②「QC 选片」**：铁律「验收不接 AI，脸漂/手畸形/调色由人终审」→ **最终裁决保留给人**，但可用 AI **预筛标红**把人从「逐张全看」降为「只看 AI 标出的疑点 + 终审选片」。

把这两点收敛好，系统就从「人盯着跑」变成「人只在该决策处出现」。

### 13.2 端到端目标流水线（操作员视角）

```
1) 新建客户 → 选输入文件夹（拖拽即可）
2) 一键「智能判图」：AI 逐张判 景别/人数/person_attrs（低置信标黄让人复核，可手改）
3) 选全册基调（自然/暖/冷）+ 场景概念池（步步生花/礁石）
4) 一键「排片」：自动选景 + 排重 + 配比检查（full 2-3/medium 3-4/close 2-3/closeup 1-2），覆盖缺口报警
5) 一键「全册出图」：批量双图出图（失败自动重试/降档策略），实时进度
6) QC：每张「原片↔候选」全分辨率对比 + AI 预筛标红 11 项疑点 → 人终审 select / 重出
7) 一键「归集」：选片归档 + 标记需放大者 → 一键送 RunningHub 高清放大 → 拼样片交付
```

### 13.3 自动化分级（务实演进，不一步到位）

| 级别 | 形态 | 对应里程碑 |
|---|---|---|
| **L1**（v1） | 手动判景别 → 自动选景/装配/出图 → 人工逐张 QC | M3 + M4 |
| **L2** | **AI 判景别**(可改) → 批量出图 → **AI 预筛 QC** + 人终审 | M4 增强 + 智能判图/预筛 |
| **L3** | 全册**一键**（判→排片→出图→预筛）→ 人只终审选片 → 一键归集+放大+拼样 + 失败自动重出 | M5 + 自动重出策略 |
| **L4** | 闭环学习：用 `picks.jsonl` 人工选片校准选景权重/判景别；成本与质量看板 | 后续 |

> 推进顺序：**L1 已跑通**（M1–M4 全部完成、模式界面可用）。接下来叠加 L2 的两个 AI 升级（判景别 / 预筛 QC，模型 `claude-sonnet-4-6`），再做 M5 串接放大与 L3/L4。

### 13.4 两个核心 AI 自动化升级（设计要点）

**① AI 判景别 + person_attrs（触点①）✅ 已接入一键流**
- 调 4sapi `/v1/messages`（Claude 原生格式），**模型用 `claude-sonnet-4-6`**（同令牌，文档见 `tips/图片理解`），输入人物原片，**结构化输出** `{shot, subjects, ...}`（当前实现先取 shot+subjects；person_attrs 余项待扩）。
- **提示词直接来自《景别判定与QC手册》§一/§七**——四档景别线索 + 8 字段 person_attrs 取值表，把"谁判都一样"的人类规则变成模型的判定标准。
- 兜底：低置信项标黄、操作员可一键改；判错由 QC 兜底（与手册同口径）。

**② AI 预筛 QC（触点②，严守"终审在人"边界）**
- 对每张候选，调视觉模型（同样 `claude-sonnet-4-6`，`/v1/messages`）按 **11 项 2C 评分单**（保本人/姿势/服装/景别占比/接地/基调/双人/手部/干净 + 比例透视/空侧无冲突）与**原片比对**，输出每项 ✓/✗/存疑 + 简述。
- **AI 只标红、不裁决**：把人从"逐张全看"降为"先看 AI 标✗的项再终审"。最终 select/重出仍由人按铁律决定。
- 边界纪律：这是**辅助预筛**，不是"AI 验收"；上线前需与负责人确认（铁律5），且预筛结论始终可被人推翻。

### 13.5 工程自动化：把 selftest 变成 Rust 回归门禁

同事用 `selftest.py` 在每次改数据/模板后离线自检。本工作台应有等价的 **Rust 回归测试**作为每次构建的门禁——部分**已落地**：
- ✅ **装配矩阵对拍**：`cmode` 的 `prompt_parity`/`rank_parity` 已覆盖「4 景别×3 基调×单双人×两 series 无残留占位符 + 与 Python 逐字一致」。
- ✅ **已补齐（M3）**：catalog 数据完整性（`catalog_integrity`：id 唯一 / plate 文件在 / `comp_prompt` 无光照天气词 / `figure_anchor` 数值合法 / `series` 非空）、四轴解耦探针（`decouple_beach_probe`：新概念不漏旧场景词）、状态机干跑（`state_machine_dryrun`：init→set→plan→assemble→失效回退）。

这样同事按 §9 推送数据/模板更新后，跑一遍 `cargo test` 即可确认未退化——把"契约不变、随便更新、你无感"真正落到工程上。

---

## 14. 现在完成了什么 / 接下来干什么

### 14.1 已完成（可验证）

- ✅ **RunningHub 批量精修/放大**：成熟可用（v1.0.0）。
- ✅ **预览放大清晰度**：对比器改全分辨率纹理，放大不发虚。
- ✅ **M1 场景大脑** `cmode.rs`：选景排序 + 四轴提示词装配，**与 Python `match_scene.py` 逐字对拍通过**。
- ✅ **M2 出图后端** `foursapi.rs`：4sapi `gpt-image-2` 双图出图，**真连出图成功**（瞬时重试/持久中止）。
- ✅ **可热插拔数据** `assets/wedding/`：模板 + catalog（18 plate）+ 配置卡（2 series）+ **18 张 plate `.jpg`**（已纳入）+ 对拍黄金样本。
- ✅ **真人端到端验证（M1+M2）**：用 `doc/1V3A3085.JPG`（红秀禾双人·中景）→ 装配（medium/暖/双人/礁石 jiaoshi_01）→ 4sapi 双图出图，**成片保本人、锁姿势/服装/拖尾、自然入礁石海景、暖金侧逆光，质量达标**。
- ✅ **M3 状态机** `cstate.rs`：manifest 全流转 + 选景排重 + 失效回退 + 复用变构图 + 选片/归集 + 持久化；移植 `selftest` 回归（状态机干跑 / catalog 完整性 / 四轴解耦探针）为 Rust 测试。
- ✅ **M4 模式界面**：顶部「RunningHub ｜ 人物入景」切换；C 模式共用外壳。
- ✅ **一键流改造**：C 模式 5 步按钮 → 单键「▶ 开始」，后台自动 判景别→选景装配→双图出图→输出夹（断点续跑/可停止/进度条）。
- ✅ **AI 判景别（触点①自动化）**：`foursapi::judge_shot`（`claude-sonnet-4-6`），**live 验证**样片→`medium/双人` 正确。
- ✅ **测试连接 + 实时额度**：`foursapi::account`（`/api/user/self`，需系统访问令牌+用户ID），**live 验证**余额 $35.46；凭据走 key.txt 三行自动读。
- ✅ **测试**：**36 个自动化测试全绿** + 4 个 live 测试（出图烟测/真人端到端/判景别/查额度，均手动已验）。

### 14.2 接下来（按依赖排序）

| 步骤 | 内容 | 完成标准 |
|---|---|---|
| ~~0 / 0.5~~ | ~~纳入 plate + 真人端到端验证~~ | ✅ **已完成** |
| ~~M3 状态机~~ | ~~manifest 全流转 + 选景排重 + QC + selftest 回归~~ | ✅ **已完成** |
| ~~M4 模式界面~~ | ~~模式切换 + 配置/列表/QC，L1 全链路~~ | ✅ **已完成** |
| **M5（下一步）** | 一条龙：人物入景出图 → 一键送 RunningHub 高清放大；可选拼样片 | 出图直接接放大产线 |
| **L2 升级** | ① AI 判景别（可改）② AI 预筛 QC（人终审）——模型 `claude-sonnet-4-6`，需负责人确认边界 | 判图/QC 提速，触点①②收敛 |
| **打磨** | C 模式 QC 用滑动/全分辨率对比；配置持久化进 Store；多候选(--candidates)；真人样片再多验几组（脸漂/景别守恒回归） | 体验完善、可对外用 |
| **L4 升级** | `picks.jsonl` 回流校准选景/判景别；成本·质量看板 | 闭环优化 |

### 14.3 立即下一步

L1 已跑通（M1–M4 完成、模式界面可用、真人出图达标）。**建议先实机跑一轮 L1**（一个真实客户文件夹走完 建账→出图→选片→归集）确认体验，再开 **M5 一条龙**（C 出图 → RunningHub 放大）或 **L2 AI 判景别/预筛 QC**（模型 `claude-sonnet-4-6`，4sapi `/v1/messages`）。

> 注：当前 C 模式 QC 预览为「原片↔候选」并排全分辨率，尚未接滑动对比。凭据（sk- 密钥/系统访问令牌/用户ID）已走 key.txt 三行自动读；文件夹/调色等仍未持久化，列入「打磨」。

---

## 15. 再分析（2026-06-28）· 对照同事 API 参考实现 + 进度刷新

> 复看同事 skill 时发现：自上次分析后，同事已**独立交付了完整的纯 API 参考实现**——正是交接文档 §4.1 / §4.2 / §5 让"我方"做的三件。两套实现互为验证，契约正确性大增。

### 15.1 同事新交付的 API 参考实现

| 新文件 | 作用 | 对应交接 | 我方对等件 |
|---|---|---|---|
| `scripts/run_one_api.py` | 纯 API 出图层（4sapi `gpt-image-2` 双图 `images.edit`，b64 落地，瞬时重试/持久中止） | §4.1 | `foursapi::edit_dual` |
| `scripts/detect_shot.py` | 景别判定（`claude-sonnet-4-6` 视觉，结构化 `{shot,subjects}`，temp=0，缩图 768） | §4.2 | `foursapi::judge_shot` |
| `c_batch.py --backend api` | 状态机出图步改调 `run_one_api.generate`；web 路径保留作"破玻璃回退" | §5 | `cstate` + `w_pipeline` |

**意义**：同一套对接契约现在有**两套独立实现**（同事 Python ＋ 我方 Rust 原生），互为参照与回归基准。

### 15.2 双实现对照（契约一致，少数实现差异）

| 环节 | 同事 Python 参考 | 我方 Rust | 结论 |
|---|---|---|---|
| 出图 | `images.edit(gpt-image-2, image=[人,plate], size, quality, n)` | `/v1/images/edits` multipart `image[]` | **一致**（SDK 即走此端点） |
| 判景别 | `chat.completions` + `image_url` data URL | `/v1/messages`（Anthropic 原生）+ base64 | **路由不同、均通**（4sapi 两路都支持；我方已 live 验） |
| 模型 | 出图 gpt-image-2 / 判景别 claude-sonnet-4-6 | 同 | **一致** |
| b64/url | 只认 b64_json，url 即报错 | b64 优先、url 兜底下载 | 我方更宽容 |
| 重试/中止 | 瞬时退避重试、持久即停 | 同 | **一致** |

### 15.3 要从参考吸收的精炼

1. **503/「渠道无货」按瞬时重试再升级持久**（同事 `classify_error`：503/502/500/429/超时→transient，重试耗尽才 permanent）。**我方 `is_transient` 目前把 503 当即时持久中止**——应改为可重试，提升断流韧性。
2. **成本与分组（重要）**：4sapi 该 key 绑「用户分组 2× ≈ **¥0.43/张**」，**不传 group、绝不走 `auto`**（auto 会路由到最贵企业组）。我方 `edit_dual` 未传 group（靠 key 绑定）＝与参考一致；应在界面/文档**标注单张成本 ≈ ¥0.43**并确认中转非 auto。一本 30 张 ≈ ¥13（外加判景别 claude 视觉每张约几分钱）。
3. **size 选档用「最近档」**（同事 `derive_size` 取 |ratio−档| 最小）；我方用阈值，比例落在 1.15~1.25 时与参考略有出入——对齐为最近档更稳。
4. **判景别路由**：我方 `/v1/messages` 已 live 通过；同事用 `chat.completions`。两者皆可、无需改，留作备用切换。

### 15.4 现在完成了什么（截至 2026-06-28）

人物入景已从"L1 手动"推进到**"一键 + AI 判景别"**，与 RunningHub 同样"填文件夹→点开始"：

- ✅ M1 大脑 / M2 出图 / M3 状态机 / M4 模式界面（全部完成，36 单测全绿）
- ✅ **一键流**：单键「▶ 开始」→ 后台 AI 判景别→选景装配→双图出图→输出夹（断点续跑/可停止/进度条）
- ✅ **AI 判景别（触点①自动化）** `claude-sonnet-4-6`，live 验证正确（样片→中景/双人）
- ✅ **测试连接 + 实时额度**（`/api/user/self`，系统访问令牌+用户ID，live 验证余额 $35.46）；凭据走 key.txt 三行自动读
- ✅ 真人端到端 / 判景别 / 查额度 / 出图烟测 共 4 个 live 测试均手动验证通过

### 15.5 接下来干什么（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| **立即·吸收参考** | 503 改瞬时重试；size 对齐"最近档"；界面/文档标注 ¥0.43/张 + 确认非 auto 分组 | 低成本、提升韧性与成本可控 |
| **高·触点②** | **AI 预筛 QC**（`claude-sonnet-4-6`，11 项评分单与原片比对，只标红、人终审） | 把 QC 从"逐张全看"降为"看疑点+选片"，收敛最后一个人工触点 |
| **高·M5** | 一条龙：C 出图（1.6MP）→ 一键送 RunningHub 高清放大 | 接上交付产线（替代 Topaz） |
| **中·打磨** | 多候选（`candidates=n`，参考已支持）、QC 滑动对比、配比/覆盖检查（手册§三/§八）、文件夹/调色记忆 | 体验完善 |
| **后·L3/L4** | 失败自动重出（脸漂 closeup→close 降档，手册§五）、`picks.jsonl` 回流校准、成本看板（¥0.43×张数预估） | 全册一键 + 闭环学习 |

### 15.6「完善的高度自动化系统」当前判断

两个不可省的人工触点中——**① 判景别已自动化（AI＋可手改）**；**② QC 选片**按铁律保留人终审（可加 AI 预筛把人力压到最小）。距离 **L3「全册一键、人只终审」**只差三块：**AI 预筛 QC ＋ M5 放大串接 ＋ 失败自动重出**。工程可信度由**两套独立实现互验 ＋ Rust 回归门禁**（装配对拍 / catalog 完整性 / 解耦探针 / 状态机干跑，§13.5 已全部落地）共同保障——这已是一个"骨架完善、只差最后两块自动化"的高度自动化系统。

> 本节的 done/next 已被 **§16（2026-06-28 生产验证后的最新快照）** 取代，以 §16 为准。

---

## 16. 快照（2026-06-28 · 生产验证后）· 完成度 + 自动化路线

> 这一节是生产验证当天的 done/next；其后又做了分发体验打磨（版本号 / 跨机一致 / 单多文件 / 进度条），
> **已被文末 §17（2026-06-29）取代，最新以 §17 为准**。本节保留作演进留痕。
> 节点：人物入景已对齐 RunningHub 的产线体验（填文件夹→点开始），并经**真实客户素材出图验证**质量达标。

### 16.1 §15 之后又完成了什么

- ✅ **并发产出**：`run_pool` worker 池，判景别 / 出图各开 N 线程（界面并发数 1–8）。
- ✅ **断点续跑**：输出按 `<原片名>_c.png` 命名（对增删图鲁棒）；已有结果**预跳过、不判不出**（省钱）；同客户再点开始**复用账本、保留手改**，关掉续跑=从头重来。
- ✅ **图片列表筛选**：全部 / 进行中 / 成功 / 跳过 / 失败 + 进度条 + 成功/跳过/失败计数（状态新增 生成中 / 已跳过 / 出图失败）。
- ✅ **「单人变双人」bug 修复**（关键）：判景别 prompt 对齐同事 `detect_shot`（人数单列、「背景路人/只露的一只手/递东西的手/倒影/道具都不算人、拿不准按 1」）+ `temperature:0`；判失败默认改「单人」；改 单/双 或景别 → 删旧图回待出图、下次「开始」只重出这一张。
- ✅ **模板清理**：`assets/wedding/templates/` 只留程序读的 `人物入景_双图暖金版.md`；弃用的文字版已删、`景别判定与QC手册.md` 移到 `doc/` 作参考。
- ✅ **生产验证通过**：真实素材 `cinput`（沙滩单人 / 游艇双人 / 秀禾单人）实跑——**人数判定 1/2/1 全对**（含秀禾原片「他人递花的手」被正确忽略、出干净单人）；`natural` 自然调色把「暖金日落 / 大晴天蓝海 / 红棚拍」三种迥异原片**全部强制压成阴天中性**；保本人 / 锁服装拖尾 / 入景接地全部达标 → **达交付水准**。
- ✅ **场景库改为局域网共享单一来源**：从「随程序打包」改为放 `\\DESKTOP-66773HC\ai_work\wedding_scene_lib`，程序默认指向（`SHARED_SCENE_DIR`）；C 模式配置（场景库路径/输出夹/调色/概念池/并发/续跑）**持久化进 `vc_batch_config.json`**（设一次记住）。**好处**：分发包只剩 exe+key.txt+使用文档（永远很小）；扩库只在共享改一次、全员生效。
- ✅ **分发就绪**：`doc/使用文档.md`（操作 + 共享场景库配置 + 一键打包，包不含场景库）。

### 16.2 当前自动化成熟度（人物入景流水线）

| 环节 | 状态 | 谁来做 / 兜底 |
|---|---|---|
| 选客户文件夹 / 输出夹 | ✅ | 操作员点两下 |
| 判景别 + 人数 | ✅ **AI 自动** | `claude-sonnet-4-6`，列表可手改重出 |
| 选景 + 排重 + 复用变构图 | ✅ 自动 | 算法（最配优先） |
| 装配提示词（四轴） | ✅ 自动 | `cmode` |
| 双图出图（并发 / 续跑） | ✅ 自动 | `foursapi` + 线程池 |
| **QC 选片** | 🟡 **人工**（铁律保留） | 操作员看对比选用/判废；**可加 AI 预筛标红** |
| 归集 / 标记需放大 | ✅ 自动 | 一键 |
| 高清放大 | ⏳ 待做 | 计划串 RunningHub（M5） |

→ **只剩 QC 选片是设计上保留的人工触点**；其余全自动。已是"填文件夹→点开始→QC 选片"的高度自动化形态。

### 16.3 现在完成了什么（汇总）

- **RunningHub 精修/放大**：成熟可用。
- **人物入景全链路**：M1 大脑 / M2 出图 / M3 状态机 / M4 一键界面，+ **并发 / 断点续跑 / 列表筛选 / AI 判景别 / 人数修复 / 实时额度**，已**生产验证**。
- **工程**：36 个自动化测试 + 4 个 live；Rust 回归门禁（对拍 / 数据完整性 / 解耦探针 / 状态机）；与同事 Python 参考实现**两套互验**。
- **可信数据契约**：模板/catalog/配置卡当外部热插拔数据，扩库只改数据；凭据走 `key.txt` 三行。
- **可分发**：使用文档 + 一键打包 zip。

### 16.4 接下来干什么（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| **立即·稳健** | 吸收同事参考的 3 处精炼（§15.3 仍未做）：**503/无货按瞬时重试再升级持久**、size 选「最近档」、界面标注 **¥0.43/张** | 改动小、提升断流韧性与成本可控 |
| **高·锁景别更紧** | 生产验证发现「中景偶尔出成接近全身」（02 游艇）：模板②号锁景别块加硬约束，或景别也走「改后单张重出」 | 减少景别漂 |
| **高·触点②** | **AI 预筛 QC**：`claude-sonnet-4-6` 按《景别判定与QC手册》11 项评分单与原片比对、只标红，人终审 | 让 QC 从"逐张全看"降为"看疑点+选片" |
| **高·M5** | 一条龙：C 出图（1.6MP）→ 一键送 RunningHub 高清放大 | 接上交付产线（替代 Topaz） |
| **中·打磨** | 多候选（`candidates=n`）、QC 滑动对比、配比/覆盖检查（手册§三/§八，避免场景复用偏多）、文件夹/调色持久化 | 体验完善 |
| **后·L3/L4** | 失败自动重出（脸漂 closeup→close 降档，手册§五）、`picks.jsonl` 回流校准选景、成本看板（¥0.43×张数） | 全册一键 + 闭环学习 |

### 16.5「完善的高度自动化系统」终态判断（刷新）

现在已经是一个**可投产、骨架完善**的高度自动化系统：操作员只需「**填文件夹 → 点开始 → 对结果 QC 选片**」，判景别/选景/装配/出图全自动，并发提速、断点续跑省钱、列表筛选可控；生产验证证明核心质量（人数、锁脸、锁服装、统一调色、入景）达标。

要达到**「全册一键、人只在终审出现」的 L3**，只差把**最后一个人工触点（QC）也用 AI 预筛减负**（终审仍在人，守铁律），再加 **M5 放大串接** 与 **失败自动重出**。再往上 **L4** 是用 `picks.jsonl` 做闭环学习 + 成本/质量看板。当前与同事 Python 参考实现互为验证、回归门禁齐备，**离"完善"只差这几块、且都是增量、不动已验证的核心**。

---

## 17. 最新快照（2026-06-29 · 分发体验打磨后）· 当前权威 done / next

> **这是当前权威的「现在完成了什么 / 接下来干什么」**，依次取代 §14 / §15 / §16 的 done/next。
> 自 §16 生产验证后，核心算法/出图/状态机**未动**（不冒险改已验证的命门），这一轮专做**分发与跨机体验打磨**：让发给同事的同一个 exe 在不同 Windows 机器上「长得一样、好认版本、选素材更顺手、进度条更顺眼」。对应版本 **v1.2.1**。

### 17.1 §16 之后又完成了什么（4 项打磨，均已编译 + 36 测试全绿 + 实机出包）

- ✅ **版本号显示**：常量 `APP_VERSION = env!("CARGO_PKG_VERSION")`（取自 `Cargo.toml`，迭代只改那一处）。**窗口标题**与**头部右上角** `v1.2.1` 都显示，同事报问题时一眼对得上版本。
- ✅ **跨机样式一致（DPI 解耦）**：解决「同一个 exe 在我和同事电脑上控件大小/布局不一样」。新增持久化 `Store.win_scale`（默认 1.0，0.7–2.0 夹取），每帧 `ctx.set_zoom_factor(win_scale / native_pixels_per_point)` **抵消各机系统 DPI 缩放**，使有效像素密度只由 `win_scale` 决定 → 跨机一致；模式条上提供 **➖ / ➕** 让操作员按自己屏幕微调并记住。
- ✅ **单/多文件输入（两个模块都加）**：原来只能「选整个文件夹」。新增 `pick_image_files()`（原生对话框，**支持单选/多选**图片）+ `is_image_file()` 过滤，并支持**直接把图片拖进来**；这些**单独选/拖入的图片与文件夹叠加去重**一起处理。RunningHub 与人物入景**两个模块都支持**；人物入景头部显示「➕ 已单独选 N 张原片」并可一键清空。`w_input_files: Vec<PathBuf>` 持有这批散图，跑批时与文件夹扫描结果合并。
- ✅ **进度条平滑动画**（v1.2.1，两模块共用 `anim_progress()`）：进度条不再死跟真实进度——**跑动中**以真实进度的 90%（`real*0.9`）为下限，叠加一条带正弦抖动的「涓流」缓慢上爬、**封顶 95%**（越接近越慢 + 抖动＝自然「卡」在最后一段，视觉上一直在动）；**真完成后**吸附到 100%（`max(当前, real)`，绝不回退）。仅在跑动中按 ~30fps 重绘，停下 1 帧即 settle——**不会让 headless UI 测试无限重绘**（这点踩过坑：第一版「动画未满也持续重绘」导致 `Harness::run` 超步数，3 个测试红，改成「停下即吸附」后全绿）。计数文本始终显示真实数。
- ✅ **分发刷新**：版本 `1.2.0 → 1.2.1`，仓库内中文名 release exe 同步，桌面分发包重打为 `视觉海岸批量处理工作台_v1.2.1`（exe + key.txt + 使用文档.md）及同名 zip，**旧 v1.2.0 文件夹与 zip 已删**。

> 这一轮的工程意义：把「一个 exe 发给不同机器的人用」这件事打磨到位（版本可追、样式一致、选素材灵活、进度观感好），是**从「我自己能跑」到「能交付给同事用」的最后一段体验差**，不触碰已验证的出图核心。

### 17.2 当前代码规模与测试（核对实测）

| 项 | 实测值 |
|---|---|
| 模块行数 | `main.rs` ~4841 · `cmode.rs` 724 · `cstate.rs` 573 · `foursapi.rs` 585 · `api.rs` 344 · `workflow.rs` 136（合计 ~7200） |
| 测试函数 | **40 个**（cmode 8 / cstate 2 / foursapi 7 / main 21 / workflow 2） |
| 常规 `cargo test` | **36 通过 + 4 ignored**（4 个联网 live：出图烟测 / 查额度 / 判景别 / 真人端到端，手动已验） |
| 版本 / 产物 | `Cargo.toml` v1.2.1；release exe ≈ 9.4 MB，零运行时依赖 |

### 17.3 自动化成熟度（不变：QC 选片仍是唯一保留的人工触点）

打磨这一轮不改自动化等级，沿用 §16.2 结论：判景别（AI）→ 选景排重（算法）→ 装配（cmode）→ 双图出图（并发/续跑）全自动；**只有「QC 选片」按铁律保留人终审**；高清放大（M5）仍待做。形态仍是「**填文件夹 / 选图片 → 点开始 → QC 选片**」。

### 17.4 现在完成了什么（截至 2026-06-29 · 汇总）

- **RunningHub 精修/放大**：成熟可用。
- **人物入景全链路**：M1 大脑 / M2 出图 / M3 状态机 / M4 一键界面 + 并发 / 断点续跑 / 列表筛选 / AI 判景别 / 人数修复 / 实时额度，已**生产验证**达交付水准。
- **分发与跨机体验（本轮）**：版本号显示 + 跨机 DPI 一致 + 单/多文件输入（两模块）+ 进度条平滑动画 + v1.2.1 分发包。
- **工程**：40 个测试（36 自动 + 4 live）；Rust 回归门禁（装配对拍 / catalog 完整性 / 解耦探针 / 状态机干跑）；与同事 Python 参考实现两套互验。
- **可分发**：`doc/使用文档.md` + 一键打包；场景库走局域网共享单一来源（包永远小）。

### 17.5 接下来干什么（按优先级；§16.4 列出的功能项仍全部未做）

> 提醒：本轮只动了分发/体验，**§16.4 的功能性待办一件都没做**，依然是下一步的主体。

| 优先级 | 事项 | 说明 / 完成标准 |
|---|---|---|
| **立即·稳健（低成本）** | 吸收同事参考 3 处精炼：① `is_transient` 把 **503/「渠道无货」改瞬时重试**再升级持久；② size 选档对齐「**最近档**」（`|ratio−档|` 最小）；③ 界面/文档标注**单张 ≈ ¥0.43** 并确认中转非 `auto` 分组 | 提升断流韧性、成本可控；不改核心流程 |
| **高·锁景别更紧** | 生产验证见「中景偶尔出成接近全身」（游艇双人）：模板②号锁景别块加硬约束，或景别也走「改后单张重出」 | 减少景别漂 |
| **高·触点②（收敛最后人工点）** | **AI 预筛 QC**：`claude-sonnet-4-6` 按《景别判定与QC手册》11 项评分单与原片比对，**只标红、人终审**（守铁律5，上线前与负责人确认边界） | QC 从「逐张全看」降为「看疑点 + 选片」 |
| **高·M5 一条龙** | 人物入景出图（1.6MP）→ **一键送 RunningHub 高清放大**；可选拼样片 | 接上交付产线、替代 Topaz |
| **中·打磨** | 多候选（`candidates=n`，参考已支持）、QC 滑动对比、配比/覆盖检查（手册§三/§八，避免某场景复用偏多）、调色/文件夹记忆已做、可加运行历史 | 体验完善 |
| **后·L3/L4** | 失败自动重出（脸漂 closeup→close 降档，手册§五）、`picks.jsonl` 回流校准选景权重、成本/质量看板（¥0.43 × 张数预估） | 全册一键 + 闭环学习 |

### 17.6「完善的高度自动化系统」终态判断（2026-06-29 刷新）

系统现在是一个**可投产、骨架完善、且已打磨到能放心发给同事用**的高度自动化工作台：

- **自动化主体已成形**：操作员只需「填文件夹 / 选图片 → 点开始 → QC 选片」，判景别 / 选景 / 装配 / 出图全自动，并发提速、断点续跑省钱、列表筛选可控；真实素材出图（人数 / 锁脸 / 锁服装 / 统一调色 / 入景）已验证达标。
- **交付体验已就位**：单 exe、零依赖、跨机样式一致、版本可追、选素材灵活、进度观感顺、局域网共享场景库扩库即生效。
- **离「完善（L3：全册一键、人只终审）」仍差三块**，且都是**增量、不动已验证核心**：① **AI 预筛 QC** 把最后一个人工触点减负；② **M5 放大串接** 接上交付产线；③ **失败自动重出**。再往上 **L4** 是 `picks.jsonl` 闭环学习 + 成本看板。
- **建议的下一步**：先做「立即·稳健」三小项（503 韧性 / size 最近档 / ¥0.43 标注，半天级、收益明确），再在 **M5 放大串接** 与 **AI 预筛 QC** 中选一个主攻——前者补全交付产线、后者收敛最后人工触点，两者做完即跨入 L3。
